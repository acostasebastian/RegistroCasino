@* @model IEnumerable<Cajeros>

@{
    ViewData["Title"] = "Lista de Cajeros";
}

<br />
<br />
<div class="border">
    <div class="row">
        <div class="col-6">
            <h2 class="text-center">Lista de Cajeros</h2>
        </div>
        <div class="col-6">
            <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-info">
                <i class="fas fa-plus"></i> &nbsp; Registra un nuevo cajero
            </a>
        </div>
    </div>

    @if (Model.Count() > 0)
    {
        <table class="table table-striped table-bordered" style="width: 100%">
            <thead>
                <tr>
                   
                    <th>@Html.DisplayNameFor(m => m.Nombre)</th>
                    <th>@Html.DisplayNameFor(m => m.Apellido)</th>
                    <th>@Html.DisplayNameFor(m => m.Email)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                       
                        <td>@Html.DisplayFor(m => item.Nombre)</td>
                        <td>@Html.DisplayFor(m => item.Apellido)</td>
                        <td>@Html.DisplayFor(m => item.Email)</td>                        
                        <td>

                            <!-- Para bloquear un cajero -->
                            @if (item.LockoutEnd == null || item.LockoutEnd < DateTime.Now)
                            {
                                <a class="btn btn-success text-white" asp-action="Bloquear" style="width:150px" asp-route-id="@item.Id">
                                    <i class="fas fa-lock-open"></i> Bloquear
                                </a>
                            }
                            else
                            {
                            <!--  Para desbloquear un cajero -->
                                <a class="btn btn-danger text-white" asp-action="Desbloquear" style="width:150px" asp-route-id="@item.Id">
                                    <i class="fas fa-lock"></i> Desbloquear
                                </a>
                            }

                        </td>
                    </tr>
                }
            </tbody>
        </table>


    }
</div> *@


 @{
    ViewData["Title"] = "Lista de Cajeros";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-8">
            <h2 class="text-primary">Lista de Cajeros</h2>
        </div>
        <div class="col-4 text-right">
            <a asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Crear Nueva Cajero</a>
        </div>
    </div>

    <br />
    <br />
    <table id="tblCajeros" class="table table-striped table-bordered" style="width: 100%">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Email</th>                
                <th>Acciones</th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts {
    <script src="~/js/cajero.js"></script>
}
